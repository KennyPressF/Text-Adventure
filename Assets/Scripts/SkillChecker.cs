using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SkillChecker : MonoBehaviour
{
    //public bool CheckForSkillSuccess(Player player, Entry nextEntry)
    //{
    //    bool passedSkillCheck = false;

    //    switch (nextEntry.skillCheck)
    //    {
    //        case Entry.SkillChecks.Strength:
    //            passedSkillCheck = player.strength >= nextEntry.skillLevelRequired ? true : false;
    //            break;

    //        case Entry.SkillChecks.Dexterity:
    //            passedSkillCheck = player.dexterity >= nextEntry.skillLevelRequired ? true : false;
    //            break;

    //        case Entry.SkillChecks.Intelligence:
    //            passedSkillCheck = player.intelligence >= nextEntry.skillLevelRequired ? true : false;
    //            break;

    //        default:
    //            Debug.LogWarning("Triggered the DEFAULT case of this switch statement!");
    //            break;
    //    }

    //    return passedSkillCheck;
    //}
}
